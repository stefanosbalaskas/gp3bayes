% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pupil-fit.R
\name{fit_pupil_model_backend}
\alias{fit_pupil_model_backend}
\title{Fit the restricted pupil time-course model with an approved backend}
\usage{
fit_pupil_model_backend(
  specification,
  backend = c("rstan", "cmdstanr"),
  chains = 4L,
  iter = 2000L,
  warmup = 1000L,
  cores = min(2L, chains),
  seed = 2026,
  adapt_delta = 0.95,
  max_treedepth = 12L,
  refresh = 0L
)
}
\arguments{
\item{specification}{Approved pupil model specification.}

\item{backend}{\code{"rstan"} or \code{"cmdstanr"}.}

\item{chains, iter, warmup, cores, seed}{Sampling controls. Package-controlled
cores are capped at two.}

\item{adapt_delta, max_treedepth, refresh}{Fixed safe sampling controls.}
}
\value{
A \code{gp3bayes_pupil_fit}.
}
\description{
Fits only a \code{specify_pupil_timecourse_model()} specification through
\code{brms} using full MCMC sampling and either \code{rstan} or \code{cmdstanr}.
}
\section{Governance boundary}{

This interface accepts no arbitrary formula, family, Stan program,
inference algorithm, or unrestricted backend arguments. A returned fit does
not establish convergence, adequacy, predictive validity, or psychological
interpretation.
}

\examples{
# See vignette("bayesian-dynamic-pupillometry", package = "gp3bayes") for a complete workflow.
}
