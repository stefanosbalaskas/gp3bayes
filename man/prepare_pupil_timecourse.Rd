% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pupil-preparation.R
\name{prepare_pupil_timecourse}
\alias{prepare_pupil_timecourse}
\title{Prepare a pupil time course under explicit transformations}
\usage{
prepare_pupil_timecourse(
  data,
  contract,
  baseline_operation = c("none", "subtract", "divide", "proportion_change",
    "percent_change"),
  baseline_window = NULL,
  output_unit = NULL,
  scale_covariates = character(),
  max_rows = 2000000L,
  irregularity_review_cv = 0.1
)
}
\arguments{
\item{data}{Source data frame.}

\item{contract}{A \code{create_pupil_contract()} result.}

\item{baseline_operation}{One of \code{"none"}, \code{"subtract"}, \code{"divide"},
\code{"proportion_change"}, or \code{"percent_change"}.}

\item{baseline_window}{Required when \code{baseline_operation != "none"} unless
already recorded in the contract. Values use the contract's source
\code{time_unit} and are converted to canonical seconds during preparation.}

\item{output_unit}{Optional physical output unit. Only metre/millimetre
conversions are defined.}

\item{scale_covariates}{Declared numeric covariates to standardize.}

\item{max_rows}{Maximum accepted input rows.}

\item{irregularity_review_cv}{Coefficient-of-variation threshold recorded
as a sampling-irregularity review signal.}
}
\value{
A \code{gp3bayes_pupil_prepared} object. The source pupil values are
retained in \code{.pupil_source}; the modelled values are \code{.pupil_model}.
}
\description{
Validates ordering, identifiers, event time, pupil values, measurement flags,
sampling intervals, and baseline support. Only explicitly requested
deterministic transformations are applied and recorded.
}
\section{Governance boundary}{

This function does not detect or interpolate blinks, smooth traces, choose a
baseline, correct PFE, correct luminance, or automatically exclude samples.
}

\examples{
sim <- simulate_pupil_timecourse(
  n_participants = 3, trials_per_participant = 3,
  sampling_frequency = 20, seed = 11
)
contract <- create_pupil_contract(
  "pupil_mm", "participant_id", "trial_id", "event_time",
  "millimetres", 20, condition_col = "condition",
  blink_col = "blink", interpolation_col = "interpolated",
  validity_col = "valid", gaze_x_col = "gaze_x", gaze_y_col = "gaze_y",
  luminance_col = "luminance", baseline_window = c(-0.5, 0)
)
prepared <- prepare_pupil_timecourse(
  sim$data, contract, baseline_operation = "subtract"
)
prepared
}
