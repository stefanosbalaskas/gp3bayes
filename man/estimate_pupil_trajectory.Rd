% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pupil-estimands.R
\name{estimate_pupil_trajectory}
\alias{estimate_pupil_trajectory}
\title{Estimate posterior pupil trajectories}
\usage{
estimate_pupil_trajectory(
  prediction,
  probability = 0.95,
  interval = c("pointwise", "simultaneous")
)
}
\arguments{
\item{prediction}{A \code{gp3bayes_pupil_prediction}.}

\item{probability}{Credible probability.}

\item{interval}{\code{"pointwise"} or \code{"simultaneous"}.}
}
\value{
A \code{gp3bayes_pupil_trajectory}.
}
\description{
Summarises a finite, declared prediction grid with pointwise or grid-wise
simultaneous posterior bands.
}
\section{Uncertainty}{

\code{"simultaneous"} constructs a grid-wise band from the empirical posterior
maximum standardized deviation over the supplied finite grid. It is not a
universal continuous-time confidence band.
}

\examples{
grid <- data.frame(.event_time = seq(0, 1, length.out = 5))
draws <- matrix(rnorm(500), nrow = 100)
prediction <- as_pupil_prediction_draws(draws, grid, "millimetres")
estimate_pupil_trajectory(prediction)
}
