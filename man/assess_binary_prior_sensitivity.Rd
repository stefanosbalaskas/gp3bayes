% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binary-posterior-validation.R
\name{assess_binary_prior_sensitivity}
\alias{assess_binary_prior_sensitivity}
\title{Assess Binary Prior Sensitivity}
\usage{
assess_binary_prior_sensitivity(
  fit,
  scale_multipliers = c(tighter = 0.5, wider = 2),
  chains = fit$sampling$chains,
  iter = fit$sampling$iter,
  warmup = fit$sampling$warmup,
  cores = fit$sampling$cores,
  seed = fit$sampling$seed + 1000L,
  adapt_delta = fit$sampling$adapt_delta,
  max_treedepth = fit$sampling$max_treedepth,
  refresh = 0L,
  maximum_standardized_shift = 0.25,
  review_standardized_shift = 0.5,
  retain_fits = FALSE
)
}
\arguments{
\item{fit}{A \code{gp3bayes_binary_fit}.}

\item{scale_multipliers}{Named positive numeric multipliers applied to the
intercept, coefficient, and group-scale priors.}

\item{chains, iter, warmup, cores, seed, adapt_delta, max_treedepth, refresh}{Restricted sampling controls passed to \code{\link[=fit_binary_model]{fit_binary_model()}}.}

\item{maximum_standardized_shift}{Maximum absolute posterior-median shift,
divided by the reference posterior standard deviation, for a pass.}

\item{review_standardized_shift}{Maximum standardized shift for review.}

\item{retain_fits}{Whether alternative fitted objects should be retained.}
}
\value{
A \code{gp3bayes_binary_prior_sensitivity} object.
}
\description{
Refits the approved binary model under prespecified tighter and wider prior
scales and compares population-level posterior medians.
}
\details{
This function is computationally expensive. Sensitivity status describes
stability under the declared scale changes only; it does not prove prior
robustness under all defensible priors.
}
