% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pupil-simulation.R
\name{simulate_pupil_timecourse}
\alias{simulate_pupil_timecourse}
\title{Simulate deterministic hierarchical pupil time courses}
\usage{
simulate_pupil_timecourse(
  n_participants = 20L,
  trials_per_participant = 12L,
  n_items = 12L,
  sampling_frequency = 60,
  time_window = c(-0.5, 2.5),
  baseline_window = c(-0.5, 0),
  conditions = c("control", "treatment"),
  baseline_pupil = 4,
  response_amplitude = 0.45,
  condition_difference = 0.18,
  peak_latency = 0.9,
  participant_sd = 0.25,
  item_sd = 0.08,
  residual_sd = 0.08,
  ar1 = 0.55,
  blink_trial_probability = 0.15,
  blink_duration = 0.12,
  include_gaze = TRUE,
  include_luminance = TRUE,
  gaze_drift_sd = 0.002,
  luminance_amplitude = 0.12,
  seed = 2026,
  max_rows = 500000L
)
}
\arguments{
\item{n_participants}{Number of participants.}

\item{trials_per_participant}{Number of trials per participant.}

\item{n_items}{Number of crossed items; use \code{NULL} for no item effect.}

\item{sampling_frequency}{Sampling frequency in Hz.}

\item{time_window}{Two-element event-relative time window in seconds.}

\item{baseline_window}{Two-element pre-event baseline window.}

\item{conditions}{Character vector of condition labels.}

\item{baseline_pupil}{Baseline pupil level in millimetres.}

\item{response_amplitude}{Peak amplitude of the common simulated response.}

\item{condition_difference}{Additional peak amplitude in the second
condition. For more than two conditions it is multiplied by the
zero-based condition index.}

\item{peak_latency}{Time of the simulated waveform peak in seconds.}

\item{participant_sd, item_sd}{Standard deviations for simulated hierarchy.}

\item{residual_sd}{Innovation standard deviation.}

\item{ar1}{AR(1) residual coefficient with absolute value below one.}

\item{blink_trial_probability}{Probability that a trial contains one
synthetic blink/data-loss interval.}

\item{blink_duration}{Blink interval duration in seconds.}

\item{include_gaze, include_luminance}{Whether to add nuisance signals.}

\item{gaze_drift_sd}{Standard deviation of gaze drift increments.}

\item{luminance_amplitude}{Amplitude of the synthetic luminance nuisance.}

\item{seed}{Reproducibility seed.}

\item{max_rows}{Maximum allowed output rows.}
}
\value{
A \code{gp3bayes_pupil_simulation} containing \code{data} and separate \code{truth}.
}
\description{
Generates event-aligned synthetic pupil data with participant/item
heterogeneity, a smooth non-universal response waveform, AR(1) residual
dependence, optional blink/data-loss segments, gaze drift, and luminance
nuisance variation.
}
\section{Interpretation}{

The waveform is a convenient synthetic data-generating shape, not a claim
about a universal biological pupil response.
}

\examples{
sim <- simulate_pupil_timecourse(
  n_participants = 4, trials_per_participant = 4,
  sampling_frequency = 20, seed = 2026
)
head(sim$data)
}
