% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binary-workflow-foundation.R
\name{simulate_hierarchical_binary_data}
\alias{simulate_hierarchical_binary_data}
\title{Simulate Hierarchical Binary Data}
\usage{
simulate_hierarchical_binary_data(
  n_participants = 40,
  trials_per_participant = 20,
  n_items = 20,
  intercept = stats::qlogis(0.35),
  condition_effect = 0.8,
  participant_covariate_effect = 0.3,
  trial_covariate_effect = 0.15,
  interaction_effect = 0.25,
  participant_sd = 0.7,
  item_sd = 0.35,
  random_slope_sd = 0.3,
  random_slope_cor = 0,
  condition_probability = 0.5,
  balanced_condition = TRUE,
  include_items = TRUE,
  seed = 1
)
}
\arguments{
\item{n_participants}{Number of participants.}

\item{trials_per_participant}{Number of observations per participant.}

\item{n_items}{Number of crossed items when \code{include_items = TRUE}.}

\item{intercept}{Population intercept on the log-odds scale.}

\item{condition_effect}{Population condition contrast on the log-odds scale.}

\item{participant_covariate_effect}{Participant-covariate coefficient.}

\item{trial_covariate_effect}{Trial-covariate coefficient.}

\item{interaction_effect}{Condition-by-participant-covariate coefficient.}

\item{participant_sd}{Participant random-intercept standard deviation.}

\item{item_sd}{Crossed item random-intercept standard deviation.}

\item{random_slope_sd}{Participant condition-slope standard deviation.}

\item{random_slope_cor}{Correlation between participant intercepts and
condition slopes. It must lie strictly between -1 and 1.}

\item{condition_probability}{Treatment probability when
\code{balanced_condition = FALSE}.}

\item{balanced_condition}{Whether each participant receives an approximately
balanced condition sequence.}

\item{include_items}{Whether to generate a crossed item identifier.}

\item{seed}{Non-negative integer random-number seed.}
}
\value{
A \code{gp3bayes_binary_simulation} containing synthetic data, stored
truth, generated random effects, and design metadata.
}
\description{
Generates deterministic synthetic repeated-measures data from the approved
Bernoulli-logit contract. The simulator records all generating parameters,
participant effects, optional crossed item effects, and the random-number
seed. It performs no model fitting.
}
\details{
The condition is generated using \code{-0.5} and \code{0.5} internally and returned
as a factor with levels \code{control} and \code{treatment}. The data-generating model
includes one participant random intercept, one optional correlated
participant condition slope, and one optional crossed item intercept.
}
\examples{
simulation <- simulate_hierarchical_binary_data(
  n_participants = 12,
  trials_per_participant = 8,
  n_items = 6,
  seed = 2026
)

simulation
head(simulation$data)

}
