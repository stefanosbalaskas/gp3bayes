% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duration-model-fitting.R
\name{fit_duration_model}
\alias{fit_duration_model}
\title{Fit an Approved Hierarchical Lognormal Duration Model}
\usage{
fit_duration_model(
  specification,
  chains = 4L,
  iter = 2000L,
  warmup = 1000L,
  cores = .gp3b_default_cores(chains),
  seed = 1L,
  adapt_delta = 0.95,
  max_treedepth = 12L,
  refresh = 0L
)
}
\arguments{
\item{specification}{A \code{gp3bayes_duration_model_specification}.}

\item{chains}{Number of MCMC chains.}

\item{iter}{Total iterations per chain.}

\item{warmup}{Warmup iterations per chain.}

\item{cores}{Processor cores, not exceeding \code{chains}.}

\item{seed}{Non-negative integer seed.}

\item{adapt_delta}{Target NUTS acceptance probability.}

\item{max_treedepth}{Maximum NUTS tree depth.}

\item{refresh}{Console progress refresh interval.}
}
\value{
A \code{gp3bayes_duration_fit}.
}
\description{
Fits an approved strictly positive uncensored duration model using full MCMC
through the fixed \code{brms} and \code{rstan} route.
}
\details{
The likelihood is fixed to lognormal, the link to identity on the mean-log
parameter, the interface to \code{brms}, the backend to \code{rstan}, and the
algorithm to full MCMC sampling. A returned fit does not establish
convergence or posterior adequacy.
}
