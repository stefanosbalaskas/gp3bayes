% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pupil-specification.R
\name{check_pupil_prior_predictive}
\alias{check_pupil_prior_predictive}
\title{Check the approved pupil priors predictively}
\usage{
check_pupil_prior_predictive(
  specification,
  execute = FALSE,
  backend = c("rstan", "cmdstanr"),
  draws = 200L,
  chains = 2L,
  iter = 1000L,
  warmup = 500L,
  cores = min(2L, chains),
  seed = 2026,
  probability = 0.95,
  max_cells = 3000000L
)
}
\arguments{
\item{specification}{Approved pupil model specification.}

\item{execute}{Whether to run prior-only MCMC. Defaults to \code{FALSE}.}

\item{backend}{Approved backend, \code{"rstan"} or \code{"cmdstanr"}.}

\item{draws}{Number of prior predictive replicated draws to retain.}

\item{chains, iter, warmup, cores, seed}{Sampling controls. Package-controlled
cores are capped at two.}

\item{probability}{Central predictive interval probability.}

\item{max_cells}{Maximum retained draw-by-observation cells.}
}
\value{
A \code{gp3bayes_pupil_prior_predictive} evidence object.
}
\description{
Creates a governed prior-predictive plan by default. With
\code{execute = TRUE}, draws from the prior-only approved Gaussian \code{brms} model
and compares replicated pupil values with the observed model-scale range.
The check reports evidence only and never changes priors automatically.
}
\section{Governance boundary}{

This operation does not tune priors, select a favourable prior scale, or
certify a model as scientifically adequate. \code{execute = FALSE} performs no
compilation or fitting.
}

\examples{
# See vignette("bayesian-dynamic-pupillometry", package = "gp3bayes") for a complete workflow.
}
