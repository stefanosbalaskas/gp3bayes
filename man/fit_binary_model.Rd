% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binary-model-fitting.R
\name{fit_binary_model}
\alias{fit_binary_model}
\title{Fit an Approved Hierarchical Binary Model}
\usage{
fit_binary_model(
  specification,
  chains = 4L,
  iter = 2000L,
  warmup = 1000L,
  cores = .gp3b_default_cores(chains),
  seed = 1L,
  adapt_delta = 0.95,
  max_treedepth = 12L,
  refresh = 0L
)
}
\arguments{
\item{specification}{A \code{gp3bayes_binary_model_specification}.}

\item{chains}{Number of MCMC chains.}

\item{iter}{Total iterations per chain, including warmup.}

\item{warmup}{Warmup iterations per chain.}

\item{cores}{Number of processor cores. It cannot exceed \code{chains}.}

\item{seed}{Non-negative integer random-number seed.}

\item{adapt_delta}{Target acceptance probability for the No-U-Turn sampler.}

\item{max_treedepth}{Maximum tree depth for the No-U-Turn sampler.}

\item{refresh}{Console progress refresh interval. Use zero to suppress
iteration progress output.}
}
\value{
A \code{gp3bayes_binary_fit} containing the fitted backend object,
original specification, restricted translation, and recorded sampling
settings.
}
\description{
Fits an approved binary model specification using full MCMC sampling through
the fixed \code{brms} and \code{rstan} route.
}
\details{
The function fixes the likelihood to Bernoulli, the link to logit, the
interface to \code{brms}, the sampling backend to \code{rstan}, and the algorithm to
full MCMC sampling. It does not expose arbitrary backend arguments.

A returned fit is not evidence of convergence, posterior adequacy, causal
identification, or substantive validity. Those assessments require separate
diagnostic and reporting gates.
}
\examples{
\dontrun{
fit <- fit_binary_model(
  specification,
  chains = 4,
  iter = 2000,
  warmup = 1000,
  cores = 4,
  seed = 2026
)
}

}
