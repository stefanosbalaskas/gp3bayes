% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pupil-estimands.R
\name{pupil_condition_contrast}
\alias{pupil_condition_contrast}
\title{Estimate a posterior condition-difference trajectory}
\usage{
pupil_condition_contrast(
  prediction,
  contrast,
  threshold = 0,
  probability = 0.95
)
}
\arguments{
\item{prediction}{Pupil prediction with a \code{.condition} column.}

\item{contrast}{Character vector \code{c(level_a, level_b)} defining \code{a - b}.}

\item{threshold}{Scientifically declared threshold on the pupil scale.}

\item{probability}{Credible probability.}
}
\value{
A pupil trajectory/contrast object with pointwise probabilities that
the declared contrast exceeds \code{threshold}.
}
\description{
Estimate a posterior condition-difference trajectory
}
\examples{
grid <- expand.grid(
  .event_time = seq(0, 1, length.out = 5),
  .condition = factor(c("control", "treatment"))
)
draws <- matrix(rnorm(1000), nrow = 100, ncol = nrow(grid))
prediction <- as_pupil_prediction_draws(draws, grid, "millimetres")
pupil_condition_contrast(
  prediction, contrast = c("treatment", "control"), threshold = 0.1
)
}
